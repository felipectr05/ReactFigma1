import styled from "styled-components"
export const Main = styled.main`
  min-height: calc(100vh - 9rem);
  background-color: #44874a;
  display: flex;
  align-items: center;
  justify-content: space-around;
`
